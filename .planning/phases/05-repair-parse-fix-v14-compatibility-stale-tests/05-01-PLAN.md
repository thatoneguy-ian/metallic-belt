---
phase: 05-repair-parse-fix-v14-compatibility-stale-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alphaTest/chrome-extension/content.js
  - alphaTest/foundry-module/module.json
  - alphaTest/tests/router.test.js
autonomous: true
requirements:
  - BRIDGE-01
  - CHAR-01
  - SYNC-01
  - SYNC-02
  - SYNC-03

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node --check chrome-extension/content.js` exits 0 — the content script parses and can load in a browser at all (D-01)."
    - "`npm test` from alphaTest/ reports `Test Files 3 passed (3)` and `Tests 55 passed (55)` with 0 failed — up from the verified baseline of 30 passed / 25 failed (D-01, D-03)."
    - "`foundry-module/module.json` declares `compatibility.maximum` as the generation string `\"14\"`, so Foundry core's hard-enforced install gate permits install and enable on current stable 14.367 and on every later 14.x patch (D-02)."
    - "`foundry-module/scripts/main.js` still contains `actor.update(updates, { ddbBridgeSync: true })` exactly once — the echo-suppression loop guard survived the test repair rather than being deleted to make assertions pass (D-03)."
  artifacts:
    - "alphaTest/chrome-extension/content.js — modified in place (one line added; 641 lines -> 642)."
    - "alphaTest/foundry-module/module.json — modified in place (4 values changed; 12 top-level keys unchanged)."
    - "alphaTest/tests/router.test.js — modified in place (5 assertions rewritten to the two-argument form)."
  key_links:
    - "content.js `handleIncomingState()` `try` (line 570) <-> `finally` (line 617): the `finally` is the sole release point for the `isSyncingFromFoundry` loop guard. While the `try` is unbalanced the whole file fails to parse, so no part of the extension loads — this is the single point every other defect in the phase sits downstream of."
    - "main.js line 74 consumer `if (options.ddbBridgeSync) return;` <-> main.js line 458 producer `actor.update(updates, { ddbBridgeSync: true })`: the two-way-sync echo-suppression pair. Breaking either half reintroduces the Phase 4 infinite loop. The tests must assert this pair, never remove it."
    - "module.json `compatibility.maximum` <-> Foundry core package install gate: hard-enforced upstream, independent of all code defects. A fully repaired module still would not load while this value sits below current stable."
  prohibitions:
    - statement: "postMessage targetOrigin `'*'` in content.js is NOT changed this phase — deferred per D-04, tracked in CONCERNS.md 'Security: postMessage() Using * as targetOrigin'."
      status: "known-open, explicitly accepted"
      verification: "From alphaTest/: `grep -vE '^\\s*//' chrome-extension/content.js | grep -cF '}, \"*\");'` returns 3 (all three call sites still present and unmodified)."
    - statement: "`event.origin` validation is NOT added to the content.js message listener this phase — deferred per D-04, tracked in CONCERNS.md 'Security: Missing event.origin Validation on Message Receivers'."
      status: "known-open, explicitly accepted"
      verification: "From alphaTest/: `git diff --stat -- chrome-extension/content.js` shows 1 insertion and 0 deletions for this phase's change to that file."
    - statement: "`chrome-extension/rules.json` is NOT modified this phase — the response-header stripping stays exactly as it is, deferred per D-04."
      status: "known-open, explicitly accepted (necessary trade-off for the embedded iframe architecture, per CONCERNS.md)"
      verification: "From alphaTest/: `grep -cE '\"(x-frame-options|content-security-policy)\"' chrome-extension/rules.json` returns 2, and rules.json does not appear in this phase's `git diff --name-only`."
    - statement: "No `catch` clause is added to `handleIncomingState()`, and `handleIncomingState()` is NOT rewritten or re-indented — D-01 states the fix is one brace and that re-indentation is cosmetic and must not be treated as the fix."
      status: "deferred by decision"
      verification: "From alphaTest/: `wc -l < chrome-extension/content.js` returns 642 (baseline 641 + exactly one inserted line), and `sed -n '574p' chrome-extension/content.js` still returns the two-space-indented `  if (typeof diff.hp === \"number\") {`."
    - statement: "`inspect-chrome*.js` and `dom-result.json` are NOT removed from version control this phase — deferred per 05-CONTEXT.md Deferred Ideas."
      status: "deferred by decision"
      verification: "From alphaTest/: `ls inspect-chrome.js dom-result.json` succeeds (files still tracked and present)."
    - statement: "No AppV1 deprecation migration is performed in `foundry-module/scripts/embedded-sheet.js` — deferred per 05-CONTEXT.md Deferred Ideas (AppV1 removal is scheduled for Foundry v16; still functional in v14)."
      status: "deferred by decision"
      verification: "`foundry-module/scripts/embedded-sheet.js` does not appear in this phase's `git diff --name-only`."
---

<objective>
Restore the D&D Beyond <-> Foundry bridge to a working, installable, testable state after the failed
implementation recorded in commit `db1ad7d`. Three already-diagnosed defect classes are in scope and
nothing else: the `content.js` parse error (D-01), the Foundry v14 compatibility ceiling that blocks
installation (D-02), and the five stale `router.test.js` assertions (D-03).

This is repair work only. It introduces no new capability, no hardening, and no refactor. Per D-04 all
security items in CONCERNS.md are explicitly deferred this phase and their continued presence must not
be treated as a phase failure — see `must_haves.prohibitions` and `<threat_model>` below.

Purpose: The project's own STATE.md claims 5/5 requirements shipped, but in practice today the module
cannot be installed on current Foundry stable and the content script cannot be loaded by a browser at
all. This plan makes that claim true again. It restores BRIDGE-01, CHAR-01, SYNC-01, SYNC-02, and
SYNC-03 to a working state; it does not introduce any new requirement.

Output: Three files modified in place. No new files, no new exported symbols.

**Working directory:** every command in this plan runs from `alphaTest/` (i.e. `C:/ai-workspace/alphaTest`),
not from the repository root at `C:/ai-workspace`. File paths inside tasks are given relative to `alphaTest/`.

**Verified baseline (measured 2026-08-25, at commit 713f674):**
- `node --check chrome-extension/content.js` -> exit 1, `SyntaxError: Unexpected token 'finally'` at line 617
- `npx vitest run` -> `Test Files 2 failed | 1 passed (3)`, `Tests 25 failed | 30 passed (55)`
- `chrome-extension/content.js` is 641 lines
- `foundry-module/module.json` has 12 top-level keys and `compatibility` `{"minimum":"12","verified":"14.359","maximum":"14.363"}`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/05-repair-parse-fix-v14-compatibility-stale-tests/05-CONTEXT.md
@.planning/phases/05-repair-parse-fix-v14-compatibility-stale-tests/05-PATTERNS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Close the unbalanced try block in handleIncomingState() — the whole phase sits downstream of this</name>

  <precondition>
    Node.js and the repo's dev dependencies are installed: from `alphaTest/`, `node --version` succeeds
    and `node_modules/.bin/vitest` exists. If `node_modules/.bin/vitest` is absent, run `npm install`
    from `alphaTest/` before starting. Halt and report if Node is unavailable.
  </precondition>

  <files>alphaTest/chrome-extension/content.js</files>

  <read_first>
    - `alphaTest/chrome-extension/content.js` lines 566-622 — the whole of `handleIncomingState()`, so the
      brace structure is visible before editing.
    - `.planning/phases/05-repair-parse-fix-v14-compatibility-stale-tests/05-PATTERNS.md` section 1 — the
      verified line-by-line brace table for this exact function, and the D-01 constraint that
      re-indentation is not the fix.
    - `.planning/codebase/CONCERNS.md` section "Syntax Error: Unbalanced Braces in content.js
      handleIncomingState()" — root cause and confirmation that one brace makes `node --check` pass.
  </read_first>

  <reversibility rating="reversible">
    A single inserted line in one file; `git checkout -- chrome-extension/content.js` restores the prior
    state exactly. No data migration, no external system, no published artifact.
  </reversibility>

  <action>
    Per D-01, insert exactly one new line containing a closing brace at two-space indent, immediately
    ABOVE the existing line 617 (`  } finally {`), inside `handleIncomingState()`.

    That newly inserted brace closes the `try` opened on line 570. The brace already present on the
    `finally` line continues to close the `if (diff.spellSlots) {` block opened on line 583 — which is
    exactly why the file currently fails to parse: `try` is left open when `finally` is reached. After
    the insertion the file is 642 lines and the `finally` clause, the `setTimeout` guard release on line
    620, and the function's closing brace all keep their existing text.

    Change nothing else. Specifically:
    - Do NOT re-indent lines 574-616. D-01 states the shallow indent is a cosmetic artifact of the
      `db1ad7d` edit, that JavaScript is not whitespace-sensitive, and that re-indenting must not be
      treated as the fix. An acceptance criterion below asserts line 574 keeps its current two-space
      indent, so re-indenting will fail the task.
    - Do NOT add a `catch` clause. Deferred per D-04 and 05-PATTERNS.md section 6.
    - Do NOT alter the early `return` on line 572. It sits inside the `try`; `finally` still runs on an
      early return, so the loop guard is still released. 05-PATTERNS.md section 1 flags this explicitly
      as something not to "fix".
    - Do NOT rewrite, extract, or restructure any part of `handleIncomingState()`. This is a one-line
      insertion into a 641-line file.
    - Do NOT touch the three `window.parent.postMessage` call sites or the message listener. Deferred
      per D-04.
  </action>

  <verify>
    <automated>From `alphaTest/`: `node --check chrome-extension/content.js` — exits 0. Baseline is exit 1 with `SyntaxError: Unexpected token 'finally'`.</automated>
    <automated>From `alphaTest/`: `npx vitest run tests/content.test.js --reporter=basic` — reports `Tests  20 passed (20)` and 0 failed. Baseline is 20 failed with "Failed to parse source for import analysis".</automated>
  </verify>

  <acceptance_criteria>
    - `node --check chrome-extension/content.js` exits with code 0.
    - `npx vitest run tests/content.test.js --reporter=basic` prints `Tests  20 passed (20)` and reports 0 failed. No file under `tests/` was edited to achieve this.
    - `wc -l < chrome-extension/content.js` returns 642 (baseline 641 plus exactly one inserted line) — proves a one-line insertion, not a rewrite.
    - `sed -n '574p' chrome-extension/content.js` returns the line `  if (typeof diff.hp === "number") {` with its leading two spaces intact — proves lines 574-616 were not re-indented (D-01).
    - `grep -vE '^\s*//' chrome-extension/content.js | grep -cF '}, "*");'` returns 3 — the three deferred postMessage call sites are untouched (D-04 fence).
  </acceptance_criteria>

  <done>
    `chrome-extension/content.js` parses under `node --check` with exit 0, all 20 tests in
    `tests/content.test.js` pass without any test file being edited, and the file grew by exactly one
    line with no re-indentation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Widen module.json compatibility so the module installs on Foundry 14.367</name>

  <files>alphaTest/foundry-module/module.json</files>

  <read_first>
    - `alphaTest/foundry-module/module.json` — the full manifest, so the `compatibility` block and the
      `relationships.systems` dnd5e entry are both in view before editing.
    - `.planning/phases/05-repair-parse-fix-v14-compatibility-stale-tests/05-PATTERNS.md` section 2 — the
      verified current values and the stated target block.
    - `.planning/codebase/CONCERNS.md` section "Upstream Verification — Foundry VTT (checked 2026-08-25)"
      — the dated table establishing that current stable is 14.367, that `compatibility.maximum` is hard
      enforced while `verified` is advisory, and that Foundry v14 requires dnd5e 5.3.x.
  </read_first>

  <reversibility rating="reversible">
    Four string values in a manifest file; `git checkout -- foundry-module/module.json` restores them.
    Nothing is published from this plan, so no downstream consumer has seen the widened range.
    Raising `minimum` from "12" to "13" drops a support claim that CONCERNS.md records as untested and
    spanning two AppV1 deprecation cycles; if a v12 user ever surfaces, the value is a one-line revert.
  </reversibility>

  <action>
    Per D-02, edit the top-level `compatibility` object so its three values become the strings
    `minimum` = "13", `verified` = "14.367", `maximum` = "14". Current values are "12", "14.359", and
    "14.363" respectively.

    The ceiling must be the bare generation string "14", not a build number such as "14.367". D-02 calls
    for the generation number precisely so that Foundry patch releases after 14.367 do not re-break
    installation — the current "14.363" ceiling is exactly how the module became unloadable. Per the
    CONCERNS.md upstream table, `compatibility.maximum` is hard enforced by Foundry core and restricts
    which versions may install AND enable a package, whereas `verified` is advisory only and safe to
    raise. The `minimum` value of "13" is the target stated in 05-PATTERNS.md section 2.

    Additionally, under `relationships.systems`, in the single entry whose `id` is "dnd5e", change its
    nested `compatibility.minimum` from "3.0.0" to "5.3.0". This is the one-line manifest change
    permitted by the Claude's-Discretion clause in 05-CONTEXT.md, and it is taken because the CONCERNS.md
    upstream table records that Foundry v14 requires dnd5e 5.3.x and that 5.2.5 is not v14-compatible —
    so a 3.0.0 floor cannot guarantee a v14-capable system. Do not add an upper bound to the dnd5e
    entry; only the floor moves.

    Do not add, remove, rename, or reorder any other manifest key. The file must still have exactly 12
    top-level keys. Leave `id`, `title`, `description`, `version`, `authors`, `manifest`, `download`,
    `esmodules`, `styles`, and `flags` byte-identical. Do not bump the module `version`.
  </action>

  <verify>
    <automated>From `alphaTest/`: `node -e "const c=JSON.parse(require('fs').readFileSync('foundry-module/module.json','utf8')).compatibility; if(c.minimum!=='13'||c.verified!=='14.367'||c.maximum!=='14'){console.error('FAIL '+JSON.stringify(c));process.exit(1)} console.log('OK')"` — exits 0 and prints OK. Baseline exits 1.</automated>
    <automated>From `alphaTest/`: `node -e "const m=JSON.parse(require('fs').readFileSync('foundry-module/module.json','utf8')); const d=m.relationships.systems.find(s=>s.id==='dnd5e'); if(Object.keys(m).length!==12||d.compatibility.minimum!=='5.3.0'){console.error('FAIL');process.exit(1)} console.log('OK')"` — exits 0 and prints OK.</automated>
    <human-check>Advisory only, NOT a gate for this phase: install the module on a live Foundry 14.367 instance and confirm it can be enabled. The automated checks above prove the manifest no longer declares a ceiling below current stable, which is the diagnosed install blocker; only a running instance can confirm end-to-end install. CONCERNS.md also records one unverified open question — whether Foundry v14 serves a `frame-src`/`child-src` CSP that would block the embedded iframe from the Foundry side. That is out of scope here and is not a pass/fail condition for this phase.</human-check>
  </verify>

  <acceptance_criteria>
    - `foundry-module/module.json` parses as valid JSON (both `node -e` commands above run without a parse throw).
    - `compatibility` equals exactly `{"minimum":"13","verified":"14.367","maximum":"14"}` — the first `node -e` command exits 0.
    - The dnd5e entry under `relationships.systems` has `compatibility.minimum` equal to `"5.3.0"` and no `maximum` key added.
    - The manifest still has exactly 12 top-level keys — the second `node -e` command exits 0.
    - `git diff --numstat -- foundry-module/module.json` shows at most 4 changed lines each way; no key was added or removed.
  </acceptance_criteria>

  <done>
    The manifest declares a generation-level ceiling of "14" so Foundry core's hard-enforced install gate
    admits 14.367 and every later 14.x patch, the dnd5e floor names a v14-capable system version, and no
    other manifest key changed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update the five stale router assertions to the two-argument form and prove the full suite green</name>

  <files>alphaTest/tests/router.test.js</files>

  <read_first>
    - `alphaTest/tests/router.test.js` lines 220-320 — the five STATE_SYNC `it()` blocks and their
      assertions, plus the surrounding formatting style.
    - `.planning/phases/05-repair-parse-fix-v14-compatibility-stale-tests/05-PATTERNS.md` section 4 —
      carries the verified assertion line numbers and the exact "Fix shape" formatting to match.
    - `alphaTest/foundry-module/scripts/main.js` lines 70-80 and 450-462 — the consumer/producer pair for
      the loop-guard flag, so it is clear the code is correct and the tests are what is stale (D-03).
    - `.planning/codebase/CONCERNS.md` — the upstream table row confirming `Actor#update(data, options)`
      is unchanged in Foundry v14.
  </read_first>

  <reversibility rating="reversible">
    Assertion-only edits in a single test file; `git checkout -- tests/router.test.js` restores them. No
    production code path is touched by this task.
  </reversibility>

  <action>
    Per D-03, update the five `expect(mockActor.update).toHaveBeenCalledWith(...)` assertions inside
    `describe("Foundry Module Message Router")` so each expects TWO arguments instead of one. The five
    assertions begin at lines 233 (HP), 256 (spell slots), 277 (AC), 296 (speed), and 314 (initiative);
    these line numbers are unaffected by Tasks 1 and 2, which touch different files.

    For each of the five: keep the existing updates object exactly as written — same keys, same values,
    same order — as the first argument, and add a second argument that is an object literal whose single
    key is `ddbBridgeSync` with the value `true`. Match the formatting shown under "Fix shape" in
    05-PATTERNS.md section 4; do not invent a different assertion style, and do not substitute
    `expect.objectContaining`, `expect.anything`, `toHaveBeenCalledTimes`, or a partial matcher for the
    literal two-argument form.

    Edit only those five. Leave the two `mockTokenUpdate` assertions at lines 366 and 418 exactly as they
    are — they pass today and their call site takes a single argument. Leave the six
    `rollSavingThrow` / `rollAbilitySave` / `rollAbilityCheck` / `rollAbilityTest` / `rollSkill`
    assertions untouched.

    Do NOT modify `foundry-module/scripts/main.js`. D-03 is explicit that the loop-guard flag it passes
    is correct and must not be removed, and the CONCERNS.md upstream table confirms the two-argument
    `Actor#update(data, options)` signature is unchanged in Foundry v14. Removing the flag would
    reintroduce the Phase 4 infinite sync loop that its consumer at main.js line 74 exists to prevent. An
    acceptance criterion below greps for that call site as a regression guard.
  </action>

  <verify>
    <automated>From `alphaTest/`: `npx vitest run tests/router.test.js --reporter=basic` — reports `Tests  21 passed (21)` and 0 failed. Baseline is 5 failed / 16 passed.</automated>
    <automated>From `alphaTest/`: `npm test` — reports `Test Files  3 passed (3)` and `Tests  55 passed (55)` with 0 failed. Baseline is 2 failed | 1 passed and 25 failed | 30 passed.</automated>
    <automated>From `alphaTest/`: `grep -vE '^\s*//' foundry-module/scripts/main.js | grep -cF 'actor.update(updates, { ddbBridgeSync: true })'` — returns 1 (loop-guard regression guard).</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/router.test.js --reporter=basic` prints `Tests  21 passed (21)` and reports 0 failed.
    - `npm test` prints `Test Files  3 passed (3)` and `Tests  55 passed (55)` and reports 0 failed.
    - `grep -vE '^\s*//' foundry-module/scripts/main.js | grep -cF 'actor.update(updates, { ddbBridgeSync: true })'` returns 1 — the loop-guard producer survived (D-03).
    - `grep -c 'options.ddbBridgeSync' foundry-module/scripts/main.js` returns 1 — the loop-guard consumer at line 74 survived (D-03).
    - `git diff --name-only` for this phase lists exactly three paths — `chrome-extension/content.js`, `foundry-module/module.json`, `tests/router.test.js` — and does NOT list `chrome-extension/rules.json`, `foundry-module/scripts/main.js`, or `foundry-module/scripts/embedded-sheet.js` (D-04 fence).
    - `grep -cE '"(x-frame-options|content-security-policy)"' chrome-extension/rules.json` returns 2 — the deferred header-stripping rules are still present and unmodified (D-04 fence).
  </acceptance_criteria>

  <done>
    All 21 router tests pass, the full suite is 55/55 green across 3 files, the `ddbBridgeSync` loop-guard
    pair in main.js is intact and unmodified, and the phase diff is confined to exactly three files.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**This phase produces no new files and no new exported symbols.** Every target already exists and is
modified in place. There is no new module, no new function, no new class, no new configuration key, no
new test file, and no new public interface for a later phase to consume.

| Path | Change | New symbols |
|---|---|---|
| `alphaTest/chrome-extension/content.js` | 1 line inserted (641 -> 642 lines); closes the `try` in `handleIncomingState()` | none |
| `alphaTest/foundry-module/module.json` | 4 string values changed inside existing keys; 12 top-level keys unchanged | none |
| `alphaTest/tests/router.test.js` | 5 existing assertions rewritten to the two-argument form | none |

Downstream phases inherit no new API surface from this work. What they inherit is the pre-existing
surface actually functioning: `handleIncomingState()` loadable, the module installable on Foundry
14.367, and the `ddbBridgeSync` echo-suppression contract asserted rather than assumed.
</artifacts_this_phase_produces>

<threat_model>
## Scope statement

Per D-04 the user directed that no security work happen this phase: *"we aren't trying to raise any
security items right now."* This block is therefore a **record, not a work item**. It does two things:
it documents the known-open threats carried forward so they are not lost, and it assesses whether this
phase's own four edits introduce anything new. No task in this plan mitigates a security threat, and
none should be added on the strength of this section.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| D&D Beyond page -> content.js (ISOLATED world) | Untrusted page DOM is scraped by the content script. Unchanged this phase. |
| content.js <-> Foundry parent frame (`postMessage`) | Cross-document channel carrying character ID, character JSON, roll actions, and HP/AC/initiative state. Unchanged this phase. |
| Foundry `main.js` message router -> `Actor#update` | Inbound bridge messages are translated into document writes on a Foundry Actor. Unchanged this phase. |
| Chrome `declarativeNetRequest` (`rules.json`) -> D&D Beyond responses | Response security headers are stripped so the sheet can be framed. Unchanged this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `chrome-extension/content.js` — `window.parent.postMessage(..., "*")` at 3 call sites | high | accept | **Deferred by user decision D-04, not fixed here.** Any framing origin can receive character ID, full character JSON, roll actions, and HP/AC/initiative state. Carried forward in CONCERNS.md "Security: postMessage() Using * as targetOrigin", which records the intended fix (pin the target origin). A `must_haves.prohibitions` entry asserts the three call sites remain byte-unchanged so this phase cannot silently alter the posture in either direction. |
| T-05-02 | Spoofing | `chrome-extension/content.js` — message listener with no `event.origin` check | high | accept | **Deferred by user decision D-04, not fixed here.** `source` and `characterId` in `event.data` are attacker-controllable from any origin, so forged STATE_UPDATE messages are accepted. Carried forward in CONCERNS.md "Security: Missing event.origin Validation on Message Receivers". Task 1 explicitly forbids touching the listener. |
| T-05-03 | Tampering | `chrome-extension/rules.json` — removal of `x-frame-options` and `content-security-policy` from D&D Beyond responses | high | accept | **Deferred by user decision D-04, not fixed here.** CONCERNS.md assesses this as a necessary trade-off for the embedded-iframe architecture rather than a defect: it shifts all trust onto the Foundry origin. Not modifiable without abandoning the architecture. A `must_haves.prohibitions` entry asserts the file is absent from the phase diff. |
| T-05-04 | Elevation of Privilege | `chrome-extension/manifest.json` — `shim.js` running in the MAIN world | low | accept | **Out of scope and unchanged.** CONCERNS.md rates this low: the shim only polyfills `crypto.randomUUID` / `crypto.subtle.digest` on non-HTTPS origins and takes no external input. No task in this plan touches `manifest.json` or `shim.js`. |
| T-05-05 | Denial of Service | `foundry-module/module.json` — widened `compatibility` ceiling (this phase's own change, D-02) | low | accept | **Assessed as introducing no new threat.** `compatibility.maximum` is an install-time gate enforced by Foundry core against its own version; it grants the module no capability and reaches no attacker-controlled input. Widening "14.363" -> "14" only stops Foundry from refusing to enable the package. The paired `minimum` raise 12 -> "13" and the dnd5e floor raise 3.0.0 -> "5.3.0" both *narrow* the accepted environment, which cannot enlarge attack surface. |
| T-05-06 | Tampering | `chrome-extension/content.js` — the inserted closing brace (this phase's own change, D-01) | low | accept | **Assessed as introducing no new threat.** The edit adds one `}` that closes an already-written `try`. It introduces no new call, no new network or message path, no new data handling, and no new input source. Its net effect is that previously-unreachable code becomes reachable — code that shipped and was reviewed prior to `db1ad7d`, whose open items are already enumerated as T-05-01 and T-05-02 above. |
| T-05-07 | Tampering | `tests/router.test.js` — the five rewritten assertions (this phase's own change, D-03) | low | accept | **Assessed as reducing risk, introducing none.** Test-only edits ship nothing. The change strengthens a guard: the assertions now pin `actor.update(updates, { ddbBridgeSync: true })`, so a future edit that drops the echo-suppression flag and reintroduces the Phase 4 infinite sync loop fails the suite instead of passing it. Task 3 forbids editing `main.js` and greps both halves of the loop-guard pair as a regression check. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | **Not applicable — this phase installs no packages.** No task adds or upgrades a dependency. `npm test` and `npx vitest` both resolve the already-present `node_modules/.bin/vitest` (vitest 1.6.0, in `devDependencies`), so no registry fetch occurs and the package legitimacy gate has nothing to audit. If an executor finds itself needing `npm install`, that is a signal the working tree is incomplete, not a licence to add a dependency. |

**Net assessment:** this phase's four edits — one brace, three manifest values, five test assertions —
introduce zero new threats. T-05-01 through T-05-04 are pre-existing, are recorded here so they survive
the phase boundary, and are explicitly accepted for now under D-04. They remain tracked in
`.planning/codebase/CONCERNS.md` for a future hardening phase.
</threat_model>

<verification>
All commands run from `alphaTest/`.

**Primary gates (must all pass):**
1. `node --check chrome-extension/content.js` — exit 0. (Baseline: exit 1.)
2. `npm test` — `Test Files  3 passed (3)`, `Tests  55 passed (55)`, 0 failed. (Baseline: 2 failed | 1 passed; 25 failed | 30 passed.)
3. `node -e "const c=JSON.parse(require('fs').readFileSync('foundry-module/module.json','utf8')).compatibility; if(c.minimum!=='13'||c.verified!=='14.367'||c.maximum!=='14'){process.exit(1)}"` — exit 0.

**Regression guards (must all pass):**
4. `grep -vE '^\s*//' foundry-module/scripts/main.js | grep -cF 'actor.update(updates, { ddbBridgeSync: true })'` — returns 1.
5. `grep -c 'options.ddbBridgeSync' foundry-module/scripts/main.js` — returns 1.
6. `wc -l < chrome-extension/content.js` — returns 642.
7. `sed -n '574p' chrome-extension/content.js` — returns `  if (typeof diff.hp === "number") {` with two leading spaces.

**D-04 scope-fence checks (must all pass — these confirm deferred work was NOT done):**
8. `grep -vE '^\s*//' chrome-extension/content.js | grep -cF '}, "*");'` — returns 3.
9. `grep -cE '"(x-frame-options|content-security-policy)"' chrome-extension/rules.json` — returns 2.
10. This phase's `git diff --name-only` lists exactly `chrome-extension/content.js`, `foundry-module/module.json`, and `tests/router.test.js` — no more, no fewer.

**Explicitly NOT a failure condition:** the continued presence of every threat listed as `accept` in
`<threat_model>`. Per D-04 these are deferred, not forgotten. A verifier that flags T-05-01, T-05-02, or
T-05-03 as a phase failure has misread the scope fence.
</verification>

<success_criteria>
- `node --check chrome-extension/content.js` exits 0 (D-01).
- `npm test` from `alphaTest/` reports 55 passed, 0 failed, across 3 test files — up from the verified 30 passed / 25 failed baseline (D-01, D-03).
- All 20 `tests/content.test.js` tests pass with no edit to any file under `tests/content.test.js` (D-01).
- `foundry-module/module.json` declares `compatibility` as `{"minimum":"13","verified":"14.367","maximum":"14"}` and a dnd5e floor of `"5.3.0"`, with 12 top-level keys unchanged (D-02).
- The `ddbBridgeSync` loop-guard producer and consumer in `foundry-module/scripts/main.js` are both present and unmodified (D-03).
- The phase diff touches exactly three files; every deferred item in D-04 is verifiably untouched.
</success_criteria>

<output>
Create `.planning/phases/05-repair-parse-fix-v14-compatibility-stale-tests/05-01-SUMMARY.md` when done.

Record in the summary: the before/after `npm test` counts, the `node --check` exit code, the final
`compatibility` block, and an explicit statement that the D-04 deferred items were left untouched with
the fence-check command outputs that prove it.
</output>
